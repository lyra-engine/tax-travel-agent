---
/**
 * Herdia × Lyra entry: Marea (travel / hospitality) lives at /marea.html.
 * Fidelis editorial + tools: /fidelis, /agent, /tracker, etc.
 */
export const prerender = true;
return Astro.redirect("/marea.html", 307);
---
